/// Library surface for the `goo` build tool - asset packing/encryption,
/// compile-time ECS struct-layout codegen (via package:analyzer, hoisting
/// goo's runtime DataDescriptor layout algorithm to build time), and
/// platform build/packaging orchestration.
///
/// Placeholder - lands in Phase 4 of the project root plan. See `bin/goo.dart`
/// for the command-line entry point.
library;
