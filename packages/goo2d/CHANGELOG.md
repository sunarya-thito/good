## 0.0.1

* Slimmed down to the 2D-specific pieces (`Transform2D`, `Transform2DSystem`)
  as part of the split into `good` (dimension-agnostic kernel), `goo2d` (this
  package), and `goo2d_render` (Flutter rendering). Still an API proposal.
