/// ECS-facing Box2D physics: `RigidBody2D`/`Collider2D` components and
/// `Box2DPhysicsSystem` (backend-named per the project's naming convention -
/// a future second backend would be e.g. `Box2DLitePhysicsSystem`, not a
/// rename of this one). Runs inside the game isolate, in lockstep with
/// goo's fixed-tick scheduler.
///
/// Placeholder - lands in Phase 2 of the project root plan.
library;
