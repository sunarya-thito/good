/// Raw ffigen bindings to Steamworks' `steam_api_flat.h`. Not meant to be
/// used directly by game code - see `goo_net_steam` for the ergonomic API
/// (NetTransport/NetPeer/NetConnection) built on top of these bindings.
///
/// Placeholder - generated bindings land in Phase 3 of the project root
/// plan, once the Steamworks SDK headers are available to run ffigen
/// against. This package will also carry the prebuilt/linked native
/// library wiring per platform (likely as a Flutter FFI plugin, not a pure
/// Dart package, once that lands).
library;
